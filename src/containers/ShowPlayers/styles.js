import {
	StyleSheet
} from 'react-native';
import {
	padTop
} from '../../global/variables';

module.exports = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'stretch',
    paddingTop: padTop
  }
});
